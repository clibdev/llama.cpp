# Prepare CUDA
docker run -it --rm -v ./:/app -u $(id -u):$(id -g) nvidia/cuda:13.0.1-cudnn-devel-ubuntu22.04 /app/custom/cuda.sh

# MiniCPM-V
./llama-mtmd-cli \
    -m ggml-model-Q8_0.gguf \
    --mmproj mmproj-model-f16.gguf \
    -c 4096 --temp 0.7 --top-p 0.8 --top-k 100 --repeat-penalty 1.05 -ngl 100 \
    --image test.jpg  -p "Explain diagram"

# Gemma 3
./llama-mtmd-cli \
    -m gemma-3-12b-it-Q8_0.gguf \
    --mmproj mmproj-model-f16.gguf \
    -ngl 100 \
    --image test.jpg  -p "Explain diagram"

# Nanonets-OCR-s (https://huggingface.co/unsloth/Nanonets-OCR-s-GGUF)
./llama-mtmd-cli \
    -m Nanonets-OCR-s-BF16.gguf \
    --mmproj mmproj-F32.gguf \
    -ngl 100 \
    --image test.jpg  -p "Extract the text from the above document as if you were reading it naturally. Return the tables in html format. Return the equations in LaTeX representation. If there is an image in the document and image caption is not present, add a small description of the image inside the <img></img> tag; otherwise, add the image caption inside <img></img>. Watermarks should be wrapped in brackets. Ex: <watermark>OFFICIAL COPY</watermark>. Page numbers should be wrapped in brackets. Ex: <page_number>14</page_number> or <page_number>9/22</page_number>. Prefer using ☐ and ☑ for check boxes."
